{% set comment_count = node.field_blog_comments.comment_count %}
{% set user_name = node.getRevisionAuthor().name.value %}

{% if blog_layout == 'classic' %}
	<div class="bg2-featurebox-1">
	    <div class="col-md-8 col-sm-8 col-xs-12 pull-right">
	        <div class="postinfo-box">
	            <div class="postdate-box">
	                <div class="blog-post-info"> <span><i class="fa fa-user"></i> {{ 'By'|raw }} {{ user_name }}</span> <span><i class="fa fa-comments-o"></i> {{ comment_count }} {{ 'Comments'|raw }}</span> </div>
	            </div>
	            <h4 class="dosis uppercase title"><a href="{{ url }}">{{ node.getTitle() }} </a></h4>
	            <div class="blog-post-info"><span><i class="fa fa-comments-o"></i> {{ comment_count }} {{ 'Comments'|raw }}</span> <span><i class="fa fa-folder"></i> {{ content.field_blog_tags }}</span> </div>
	            <br/>
	            <p>{{ node.body.summary|length > 156 ? node.body.summary|slice(0,156) ~ '...' : node.body.summary }}</p>
	            <br/>
	            <br/>
	            <a class="btn btn-dark-3 btn-small" href="{{ url }}">{{ 'Read more'|raw }}</a> 
	        </div>
	    </div>
	    <div class="overlay-1"></div>
	    {% set image_url = getUrlByImageStyle(node.field_blog_image.0.entity.uri.value, 'image_1200x750') %}
	    <img src="{{ image_url }}" alt="{{ node.getTitle() }}" class="img-responsive"/> 
	</div>
{% elseif blog_layout == 'default'%}
	<div class="bg2-featurebox-3">
	    <div class="img-box">
	        <div class="postdate-box">
	            <div class="blog-post-info"> <span><i class="fa fa-user"></i> {{ 'By'|raw }} {{ user_name }}</span> <span><i class="fa fa-comments-o"></i> {{ comment_count }} {{ 'Comments'|raw }}</span> </div>
	        </div>
	        {% set image_url = getUrlByImageStyle(node.field_blog_image.0.entity.uri.value, 'image_1200x550') %}
	        <img src="{{ image_url }}" alt="{{ node.getTitle() }}" class="img-responsive"/> 
	    </div>
	    <div class="postinfo-box">
	        <h4 class="dosis uppercase title"><a href="{{ url }}">{{ node.getTitle() }} </a></h4>
            <div class="blog-post-info"><span><i class="fa fa-comments-o"></i> {{ comment_count }} {{ 'Comments'|raw }}</span> <span><i class="fa fa-folder"></i> {{ content.field_blog_tags }}</span> </div>
            <br/>
	        <p>{{ node.body.summary|length > 458 ? node.body.summary|slice(0,458) ~ '...' : node.body.summary }}</p>
	        <br/>
	        <br/>
	        <a class="btn btn-dark-3 btn-small uppercase" href="{{ url }}">{{ 'Read more'|raw }}</a> 
	    </div>
	</div>
{% elseif blog_layout == 'thumb' %}
	<div class="bg2-featurebox-4">
		{% set image_url = getUrlByImageStyle(node.field_blog_image.0.entity.uri.value, 'image_1000x800') %}
	    <div class="col-md-6 col-sm-12 col-xs-12 margin-bottom"> <img src="{{ image_url }}" alt="{{ node.getTitle() }}" class="img-responsive"/> </div>
	    <div class="col-md-6 col-sm-12 col-xs-12">
	        <h4 class="dosis uppercase title"><a href="{{ url }}">{{ node.getTitle() }} </a></h4>
	        <div class="blog-post-info"><span><i class="fa fa-comments-o"></i> {{ comment_count }} {{ 'Comments'|raw }}</span> <span><i class="fa fa-folder"></i> {{ content.field_blog_tags }}</span> </div>
            <br/>
	        <p>{{ node.body.summary|length > 458 ? node.body.summary|slice(0,458) ~ '...' : node.body.summary }}</p>
	        <br/>
	        <a class="btn btn-dark-3 btn-small" href="{{ url }}">{{ 'Read more'|raw }}</a> 
	    </div> 
	</div>
{% elseif blog_layout == '1column' %}
	<div class="bg2-featurebox-3">
	    <div class="img-box">
	        <div class="postdate-box">
	            <div class="blog-post-info"> <span><i class="fa fa-user"></i> {{ 'By'|raw }} {{ user_name }}</span> <span><i class="fa fa-comments-o"></i> {{ comment_count }} {{ 'Comments'|raw }}</span> </div>
	        </div>
	        {% set image_url = getUrlByImageStyle(node.field_blog_image.0.entity.uri.value, 'image_1200x550') %}
	        <img src="{{ image_url }}" alt="{{ node.getTitle() }}" class="img-responsive"/> 
	    </div>
	    <div class="postinfo-box">
	        <h4 class="dosis uppercase title"><a href="{{ url }}">{{ node.getTitle() }} </a></h4>
	        <div class="blog-post-info"><span><i class="fa fa-comments-o"></i> {{ comment_count }} {{ 'Comments'|raw }}</span> <span><i class="fa fa-folder"></i> {{ content.field_blog_tags }}</span> </div>
	        <br/>
	        <p>{{ node.body.summary|length > 458 ? node.body.summary|slice(0,458) ~ '...' : node.body.summary }}</p>
	        <br/>
	        <br/>
	        <a class="btn btn-dark-3 btn-small uppercase" href="{{ url }}">{{ 'Read more'|raw }}</a> 
	    </div>
	</div>
{% elseif blog_layout == '2column' %}
	<div class="col-md-6 col-sm-6 col-xs-12 apar-item-2-cl">
	    <div class="bg2-featurebox-3">
	        <div class="img-box">
	            <div class="postdate-box">
	                <div class="blog-post-info"> <span><i class="fa fa-user"></i> {{ 'By'|raw }} {{ user_name }}</span> </div>
	            </div>
	            {% set image_url = getUrlByImageStyle(node.field_blog_image.0.entity.uri.value, 'image_1200x750') %}
	            <img src="{{ image_url }}" alt="{{ node.getTitle() }}" class="img-responsive"/> 
	        </div>
	        <div class="postinfo-box">
	            <h4 class="dosis uppercase title"><a href="{{ url }}">{{ node.getTitle() }} </a></h4>
		        <div class="blog-post-info"><span><i class="fa fa-comments-o"></i> {{ comment_count }} {{ 'Comments'|raw }}</span> <span><i class="fa fa-folder"></i> {{ content.field_blog_tags }}</span> </div>
		        <br/>
		        <p>{{ node.body.summary|length > 226 ? node.body.summary|slice(0,226) ~ '...' : node.body.summary }}</p>
		        <br/>
		        <br/>
		        <a class="btn btn-dark-3 btn-small uppercase" href="{{ url }}">{{ 'Read more'|raw }}</a> 
	        </div>
	    </div>
	</div>
{% elseif blog_layout == '3column' %}
	<div class="col-md-4 col-sm-6 col-xs-12 apar-item-3-cl">
	    <div class="bg2-featurebox-3">
	        <div class="img-box">
	            {% set image_url = getUrlByImageStyle(node.field_blog_image.0.entity.uri.value, 'image_1200x750') %}
	            <img src="{{ image_url }}" alt="{{ node.getTitle() }}" class="img-responsive"/> 
	        </div>
	        <div class="postinfo-box">
	            <h4 class="dosis uppercase title"><a href="{{ url }}">{{ node.getTitle() }} </a></h4>
		        <div class="blog-post-info"><span><i class="fa fa-comments-o"></i> {{ comment_count }} {{ 'Comments'|raw }}</span> <span><i class="fa fa-folder"></i> {{ content.field_blog_tags }}</span> </div>
		        <br/>
		        <p>{{ node.body.summary|length > 97 ? node.body.summary|slice(0,97) ~ '...' : node.body.summary }}</p>
		        <br/>
		        <br/>
		        <a class="btn btn-dark-3 btn-small uppercase" href="{{ url }}">{{ 'Read more'|raw }}</a> 
	        </div>
	    </div>
	</div>
{% elseif blog_layout == '4column' %}
	<div class="col-md-3 col-sm-6 col-xs-12 apar-item-4-cl">
	    <div class="bg2-featurebox-3">
	        <div class="img-box">
	            {% set image_url = getUrlByImageStyle(node.field_blog_image.0.entity.uri.value, 'image_1200x750') %}
	            <img src="{{ image_url }}" alt="{{ node.getTitle() }}" class="img-responsive"/> 
	        </div>
	        <div class="postinfo-box">
	            <h4 class="dosis uppercase title"><a href="{{ url }}">{{ node.getTitle() }} </a></h4>
		        <div class="blog-post-info"><span><i class="fa fa-comments-o"></i> {{ comment_count }} {{ 'Comments'|raw }}</span> <span><i class="fa fa-folder"></i> {{ content.field_blog_tags }}</span> </div>
		        <br/>
		        <p>{{ node.body.summary|length > 61 ? node.body.summary|slice(0,61) ~ '...' : node.body.summary }}</p>
		        <br/>
		        <br/>
		        <a class="btn btn-dark-3 btn-small uppercase" href="{{ url }}">{{ 'Read more'|raw }}</a> 
	        </div>
	    </div>
	</div>
{% elseif blog_layout == '5column' %}
	<div class="col-md-1 col-sm-6 col-xs-12 apar-item-5-cl">
	    <div class="bg2-featurebox-3">
	        <div class="img-box">
	            {% set image_url = getUrlByImageStyle(node.field_blog_image.0.entity.uri.value, 'image_1200x750') %}
	            <img src="{{ image_url }}" alt="{{ node.getTitle() }}" class="img-responsive"/> 
	        </div>
	        <div class="postinfo-box">
	            <h4 class="dosis uppercase title"><a href="{{ url }}">{{ node.getTitle() }} </a></h4>
		        <div class="blog-post-info"><span><i class="fa fa-comments-o"></i> {{ comment_count }} {{ 'Comments'|raw }}</span> <span><i class="fa fa-folder"></i> {{ content.field_blog_tags }}</span> </div>
		        <br/>
		        <p>{{ node.body.summary|length > 61 ? node.body.summary|slice(0,61) ~ '...' : node.body.summary }}</p>
		        <br/>
		        <br/>
		        <a class="btn btn-dark-3 btn-small uppercase" href="{{ url }}">{{ 'Read more'|raw }}</a> 
	        </div>
	    </div>
	</div>
{% else %}
	<div class="col-md-2 col-sm-6 col-xs-12 apar-item-6-cl">
	    <div class="bg2-featurebox-3">
	        <div class="img-box">
	            {% set image_url = getUrlByImageStyle(node.field_blog_image.0.entity.uri.value, 'image_1200x750') %}
	            <img src="{{ image_url }}" alt="{{ node.getTitle() }}" class="img-responsive"/> 
	        </div>
	        <div class="postinfo-box">
	            <h4 class="dosis uppercase title"><a href="{{ url }}">{{ node.getTitle() }} </a></h4>
		        <div class="blog-post-info"><span><i class="fa fa-comments-o"></i> {{ comment_count }} {{ 'Comments'|raw }}</span> <span><i class="fa fa-folder"></i> {{ content.field_blog_tags }}</span> </div>
		        <br/>
		        <p>{{ node.body.summary|length > 61 ? node.body.summary|slice(0,61) ~ '...' : node.body.summary }}</p>
		        <br/>
		        <br/>
		        <a class="btn btn-dark-3 btn-small uppercase" href="{{ url }}">{{ 'Read more'|raw }}</a> 
	        </div>
	    </div>
	</div>
{% endif %}